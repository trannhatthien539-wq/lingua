import { LoaderCircle, Plus, X } from "lucide-react";
import Dialog from "../ui/Dialog";

export default function NewDeckModal({ value, onChange, onSubmit, onClose, busy }) {
  return <Dialog onClose={onClose} ariaLabelledBy="new-deck-title" overlayClassName="fixed inset-0 z-[125] grid place-items-center bg-ink/40 p-4 backdrop-blur-sm" className="panel w-full max-w-md p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="eyebrow">Bộ thẻ mới</p><h2 id="new-deck-title" className="mt-1 font-display text-xl font-bold">Đặt tên cho bộ thẻ</h2></div><button type="button" onClick={onClose} className="icon-btn -mr-2" aria-label="Đóng"><X size={18} /></button></div><form onSubmit={onSubmit} className="mt-5"><label className="block"><span className="mb-2 block text-xs font-bold text-ink/60 dark:text-white/60">Tên bộ thẻ *</span><input autoFocus required value={value} onChange={(event) => onChange(event.target.value)} placeholder="Ví dụ: 5 từ vựng giao tiếp" className="field" /></label><p className="mt-2 text-xs leading-5 text-ink/50 dark:text-white/50">Bạn có thể thêm từ thủ công hoặc dùng AI ngay sau khi tạo.</p><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={onClose} className="btn-secondary">Huỷ</button><button type="submit" disabled={!value.trim() || busy} className="btn-primary">{busy ? <LoaderCircle size={16} className="animate-spin" /> : <Plus size={16} />}Tạo bộ thẻ</button></div></form></Dialog>;
}
