# Project instructions

- Use JavaScript and React with Vite.
- Keep feature code inside `src/modules` and shared UI inside `src/components`.
- Prefer small, composable components and keep API calls inside `src/services`.
- Preserve the existing Tailwind dark mode and Vietnamese product copy.
