node_modules
dist
dev.log
# Log tạm khi chạy `npm run dev` thủ công (dev_v.log, dev_check.log…) — không commit.
dev_*.log
.env
.env.*
!.env.example
.vscode/*
!.vscode/extensions.json

# Project native được CI sinh ra khi build (xem .github/workflows/build-apk.yml).
# Mã nguồn widget/shortcut nằm ở native/android-widget/ và được chép vào lúc build.
android/
ios/
